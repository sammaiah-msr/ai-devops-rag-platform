apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "local-llm-api.fullname" . }}-config
  labels:
    {{- include "local-llm-api.labels" . | nindent 4 }}
data:
  OLLAMA_BASE_URL: {{ .Values.ollama.baseUrl | quote }}